name: smart_purchase_analyzer
description: AI 구매 후회 방지 분석기 - 소비 습관 개선 앱

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.6
  fl_chart: ^0.68.0
  google_fonts: ^6.1.0
  intl: ^0.19.0

  # State Management
  provider: ^6.1.1

  # Backend
  supabase_flutter: ^2.3.0
  google_sign_in: ^6.2.1
  http: ^1.2.0
  flutter_dotenv: ^5.1.0

  # Utils
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  assets:
    - .env
